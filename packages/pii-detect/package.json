{
  "name": "@repo/pii-detect",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "imports": {
    "#ort": {
      "browser": "./src/ort-browser.ts",
      "default": "./src/ort-node.ts"
    }
  },
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "clean": "rm -rf node_modules coverage",
    "lint": "oxlint",
    "test": "vitest run",
    "test:coverage": "vitest run --coverage",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@huggingface/transformers": "^4.2.0",
    "@repo/redact-core": "workspace:*",
    "onnxruntime-node": "1.29.0",
    "onnxruntime-web": "1.29.0"
  },
  "devDependencies": {
    "@repo/config-vitest": "workspace:*",
    "@repo/typescript-config": "workspace:*",
    "@types/node": "^26.4.0",
    "typescript": "^6.0.3",
    "vitest": "^4.1.11"
  }
}
